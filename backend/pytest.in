[pytest]
markers =
    integration: hits real external services (Ollama). Requires Ollama running
        with the model pulled (see requirements.txt / README for setup).
        Run without them via: pytest -m "not integration"